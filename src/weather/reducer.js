import * as actionTypes from './actionTypes'

const initState = {
  name: 'xzzzzzzzzzzzzz',
  age: '24'
}

const reducer = (state=initState, action) => {
  switch(action.type) {
    default: 
      return state
  }
}

export default reducer